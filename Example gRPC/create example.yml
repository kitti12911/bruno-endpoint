info:
  name: create example
  type: grpc
  seq: 4

grpc:
  url: "{{url}}"
  method: /example.v1.ExampleService/CreateExample
  methodType: unary
  message: |-
    {
      "example": {
        "name": "hello",
        "description": "just test"
      }
    }
  auth: inherit
